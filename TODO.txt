Parts del projecte PYGES:

- pagines multillenguatge (model datastore)
- site global config
    - gestió pestanyes, pagines, menus (ordre, timestamp)
- admin (auth, backend vs frontend)
    - gestió errors, forbidden page, etc. (login&)
- editor WYSIWYG (CKeditor, ...)
- disseny (page rendering) i custom CSS (skins)


- galeries de fotos (blobstore)
    - visor galeries (tiltviewer, carroussel, jQuery UI, etc.)
    - integració editor/galeries
- pagina de contacte (emails)

- categories (pagines,fotos,uploads)
- calendari events (datastore)
- integració events google calendar
- 




Site config:
- languages
- default lang
- site name
- default skin
- admin users (by email)
